diff --git a/CMakeLists.txt b/CMakeLists.txt
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -65,23 +65,4 @@ ELSEIF(APPLE)
     IF(OSG_BUILD_PLATFORM_IPHONE)
-
-        # set sdk and min versions
-        SET (IPHONE_SDKVER "10.2" CACHE STRING "IOS SDK-Version")
-        SET (IPHONE_VERSION_MIN "7.0" CACHE STRING "IOS minimum os version, use 7.0 or greater to get 64bit support")
-
-        # get full path to sdk from requested versions
-        SET (IPHONE_DEVROOT "/Applications/Xcode.app/Contents/Developer/Platforms/iPhoneOS.platform/Developer")
-        SET (IPHONE_SDKROOT "${IPHONE_DEVROOT}/SDKs/iPhoneOS${IPHONE_SDKVER}.sdk")
-
-        # optionally enable bitcode for the build
-        SET (IPHONE_ENABLE_BITCODE "NO" CACHE STRING "IOS Enable Bitcode")
-
-        # seamless toggle between device and simulator
-        SET(CMAKE_XCODE_EFFECTIVE_PLATFORMS "-iphoneos;-iphonesimulator" CACHE STRING "Xcode effective platforms for iOS")
-
-        # set deployment target to min version
-        SET(CMAKE_XCODE_ATTRIBUTE_IPHONEOS_DEPLOYMENT_TARGET "${IPHONE_VERSION_MIN}" CACHE STRING "Xcode deployment target for iOS")
-
-        # Set standard architectures
-        SET(CMAKE_OSX_ARCHITECTURES "$(ARCHS_STANDARD)" CACHE STRING "Build architectures for iOS")
-
+        SET(OSG_WINDOWING_SYSTEM "None" CACHE STRING "SDL owns the iOS graphics context" FORCE)
+        SET(IPHONE_ENABLE_BITCODE "NO" CACHE STRING "iOS bitcode is disabled")
     ELSE()
@@ -237,9 +219,8 @@ IF(APPLE)
     IF(OSG_BUILD_PLATFORM_IPHONE)
-
-        # set the sdk path as our sysroot, if we set this before the project is defined cmake fails to build its test program
-        SET(CMAKE_OSX_SYSROOT "${IPHONE_SDKROOT}" CACHE STRING "System root for iOS" FORCE)
-
-        # Set the path to OpenGL library
-        SET(OPENGL_gl_LIBRARY "${IPHONE_SDKROOT}/System/Library/Frameworks/OpenGLES.framework")
-
+        IF(NOT CMAKE_SYSTEM_NAME STREQUAL "iOS")
+            MESSAGE(FATAL_ERROR "OSG_BUILD_PLATFORM_IPHONE requires an iOS toolchain")
+        ENDIF()
+        FIND_PACKAGE(gl4es CONFIG REQUIRED)
+        SET(OPENGL_gl_LIBRARY gl4es::GL)
+        GET_TARGET_PROPERTY(OPENGL_INCLUDE_DIR gl4es::GL INTERFACE_INCLUDE_DIRECTORIES)
     ELSE ()
@@ -553,4 +534,9 @@ IF (OSG_GL3_AVAILABLE)
     IF (APPLE)
-        SET(OPENGL_HEADER1 "#include <OpenGL/gl.h>" CACHE STRING "#include<> line for OpenGL Header")
-        SET(OPENGL_HEADER2 "#include <OpenGL/gl3.h>" CACHE STRING "#include<> line for additional OpenGL Headers if required")
+        IF(OSG_BUILD_PLATFORM_IPHONE)
+            SET(OPENGL_HEADER1 "#include <GL/gl.h>" CACHE STRING "GL4ES desktop GL header")
+            SET(OPENGL_HEADER2 "" CACHE STRING "No second GL4ES header is required")
+        ELSE()
+            SET(OPENGL_HEADER1 "#include <OpenGL/gl.h>" CACHE STRING "#include<> line for OpenGL Header")
+            SET(OPENGL_HEADER2 "#include <OpenGL/gl3.h>" CACHE STRING "#include<> line for additional OpenGL Headers if required")
+        ENDIF()
     ELSE()
@@ -594,8 +580,13 @@ ELSEIF(OSG_GLES3_AVAILABLE)
 ELSE()
     IF (APPLE)
-        SET(OPENGL_HEADER1 "#include <OpenGL/gl.h>" CACHE STRING "#include<> line for OpenGL Header")
-        SET(OPENGL_HEADER2 "" CACHE STRING "#include<> line for additional OpenGL Headers if required")
+        IF(OSG_BUILD_PLATFORM_IPHONE)
+            SET(OPENGL_HEADER1 "#include <GL/gl.h>" CACHE STRING "GL4ES desktop GL header")
+            SET(OPENGL_HEADER2 "" CACHE STRING "No second GL4ES header is required")
+        ELSE()
+            SET(OPENGL_HEADER1 "#include <OpenGL/gl.h>" CACHE STRING "#include<> line for OpenGL Header")
+            SET(OPENGL_HEADER2 "" CACHE STRING "#include<> line for additional OpenGL Headers if required")
+        ENDIF()
     ELSE()
         SET(OPENGL_HEADER1 "#include <GL/gl.h>" CACHE STRING "#include<> line for OpenGL Header")
         SET(OPENGL_HEADER2 "" CACHE STRING "#include<> line for additional OpenGL Headers if required")
     ENDIF()
@@ -824,3 +815,6 @@
 IF(NOT (ANDROID AND OSG_FIND_3RD_PARTY_DEPS))
-    IF(NOT APPLE)
+    IF(OSG_BUILD_PLATFORM_IPHONE)
+        FIND_PACKAGE(JPEG REQUIRED)
+        FIND_PACKAGE(PNG REQUIRED)
+    ELSEIF(NOT APPLE)
         FIND_PACKAGE(GIFLIB)
@@ -1126,12 +1115,4 @@ IF(APPLE AND NOT ANDROID)
     IF(OSG_BUILD_PLATFORM_IPHONE)
-
-        # set osg window system to iOS
-        SET(OSG_WINDOWING_SYSTEM "IOS" CACHE STRING "Windowing system type for graphics window creation, options only IOS." FORCE)
-
-        # set osg default image plugin
-        SET(OSG_DEFAULT_IMAGE_PLUGIN_FOR_OSX "imageio" CACHE STRING "Forced imageio default image plugin for iOS" FORCE)
-
-        # ensure app bundles for example app
-        OPTION(OSG_BUILD_APPLICATION_BUNDLES "Enable the building of applications and examples as OSX Bundles" ON)
-
+        SET(OSG_WINDOWING_SYSTEM "None" CACHE STRING "SDL owns the iOS graphics context" FORCE)
+        OPTION(OSG_BUILD_APPLICATION_BUNDLES "Build application bundles" OFF)
     ELSE()
diff --git a/CMakeModules/CheckAtomicOps.cmake b/CMakeModules/CheckAtomicOps.cmake
--- a/CMakeModules/CheckAtomicOps.cmake
+++ b/CMakeModules/CheckAtomicOps.cmake
@@ -19,10 +19,8 @@ ELSE()
     IF(OSG_BUILD_PLATFORM_IPHONE_SIMULATOR OR OSG_BUILD_PLATFORM_IPHONE)
-       SET(_OPENTHREADS_ATOMIC_USE_GCC_BUILTINS 0)
+       SET(_OPENTHREADS_ATOMIC_USE_GCC_BUILTINS 1)
        SET(_OPENTHREADS_ATOMIC_USE_MIPOSPRO_BUILTINS 0)
        SET(_OPENTHREADS_ATOMIC_USE_SUN 0)
        SET(_OPENTHREADS_ATOMIC_USE_WIN32_INTERLOCKED 0)
        SET(_OPENTHREADS_ATOMIC_USE_MUTEX 0)
-
-       SET(_OPENTHREADS_ATOMIC_USE_BSD_ATOMIC 1)
+       SET(_OPENTHREADS_ATOMIC_USE_BSD_ATOMIC 0)
-
     ELSE()
diff --git a/src/osg/GLExtensions.cpp b/src/osg/GLExtensions.cpp
--- a/src/osg/GLExtensions.cpp
+++ b/src/osg/GLExtensions.cpp
@@ -45,5 +45,8 @@
     #include <AvailabilityMacros.h>
-    #if !defined(MAC_OS_X_VERSION_10_3) || (MAC_OS_X_VERSION_MIN_REQUIRED < MAC_OS_X_VERSION_10_3)
+    #include <TargetConditionals.h>
+    #if TARGET_OS_IPHONE
+        #include <gl4es/gl4esinit.h>
+    #elif !defined(MAC_OS_X_VERSION_10_3) || (MAC_OS_X_VERSION_MIN_REQUIRED < MAC_OS_X_VERSION_10_3)
         #define USE_APPLE_LEGACY_NSSYMBOL
         #include <mach-o/dyld.h>
     #else
@@ -353,8 +356,9 @@
         #endif
-
+
     #elif defined(__APPLE__)
-
-        #if defined(USE_APPLE_LEGACY_NSSYMBOL)
+        #if TARGET_OS_IPHONE
+            return gl4es_GetProcAddress(funcName);
+        #elif defined(USE_APPLE_LEGACY_NSSYMBOL)
             std::string temp( "_" );
             temp += funcName;    // Mac OS X prepends an underscore on function names
             if ( NSIsSymbolNameDefined( temp.c_str() ) )
diff --git a/src/osgDB/CMakeLists.txt b/src/osgDB/CMakeLists.txt
--- a/src/osgDB/CMakeLists.txt
+++ b/src/osgDB/CMakeLists.txt
@@ -125,7 +125,7 @@ SET(TARGET_SRC
     ${OPENSCENEGRAPH_VERSIONINFO_RC}
 )
-
+
-IF(APPLE AND NOT ANDROID)
+IF(APPLE AND NOT ANDROID AND NOT OSG_BUILD_PLATFORM_IPHONE)
     IF(NOT OSG_BUILD_PLATFORM_IPHONE)
         # Needs CoreFoundation calls and a Carbon function
         SET(OSGDB_PLATFORM_SPECIFIC_LIBRARIES ${CARBON_LIBRARY} ${COCOA_LIBRARY})
diff --git a/src/osgViewer/CMakeLists.txt b/src/osgViewer/CMakeLists.txt
--- a/src/osgViewer/CMakeLists.txt
+++ b/src/osgViewer/CMakeLists.txt
@@ -217,8 +217,8 @@ ELSEIF(${OSG_WINDOWING_SYSTEM} STREQUAL "X11")
     IF(APPLE)
         # hack for finding the iphone opengl es lib
         IF(OSG_BUILD_PLATFORM_IPHONE)
-          SET(OPENGL_INCLUDE_DIR ${IPHONE_SDKROOT}/System/Library/Frameworks)
-          SET(OPENGL_LIBRARIES ${IPHONE_SDKROOT}/System/Library/Frameworks/OpenGLES)
+          GET_TARGET_PROPERTY(OPENGL_INCLUDE_DIR gl4es::GL INTERFACE_INCLUDE_DIRECTORIES)
+          SET(OPENGL_LIBRARIES gl4es::GL)
         ELSE()
           SET(LIB_EXTRA_LIBS ${X11_X11_LIB} ${LIB_EXTRA_LIBS})
         ENDIF()
